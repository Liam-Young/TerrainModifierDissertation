using UnityEngine;
using System.Collections;
using System.Linq;

public class World : MonoBehaviour {

    //void Start () 
    //{
    //    string path = "Prefabs/Buildings";

    //    Object[] Buildings = Resources.LoadAll(path);

    //    if(Buildings.Length > 0)
    //    {
    //        for (int i = 0; i < Buildings.Length; i++)
    //        {
    //            GameObject building = Buildings[i] as GameObject;
    //            Texture2D buildingIcon = building.GetComponent<Building>().MenuIcon;
    //            Texture2D buildingIconMO = building.GetComponent<Building>().MenuIconMO;

    //            MenuSetup.BuildingIconTextures.Add(buildingIcon);
    //            MenuSetup.BuildingIconTexturesMO.Add(buildingIconMO);
    //            MenuSetup.BuildingNames.Add(building.name);
    //            MenuSetup.BuildingPaths.Add(path + "/" + building.name);
    //        }
    //    }
    //}
}
